---
// Detail panel for showing event information
---

<aside id="detail-panel" class="hidden">
  <button id="close-panel">&times;</button>
  <img id="detail-image" src="" alt="" />
  <div id="detail-content">
    <div id="detail-year"></div>
    <h2 id="detail-title"></h2>
    <div id="detail-author" class="hidden"></div>
    <div id="detail-text"></div>
    <div id="detail-gallery"></div>
  </div>
  <div id="detail-nav">
    <button id="prev-event">&larr; Prev</button>
    <button id="next-event">Next &rarr;</button>
  </div>
</aside>

<!-- Lightbox for gallery -->
<div id="detail-lightbox" class="lightbox">
  <button class="lightbox-close">&times;</button>
  <button class="lightbox-nav lightbox-prev">&larr;</button>
  <div class="lightbox-content">
    <img src="" alt="" />
    <iframe src="" title="PDF viewer"></iframe>
  </div>
  <button class="lightbox-nav lightbox-next">&rarr;</button>
  <div class="lightbox-caption"></div>
</div>

<style>
  #detail-panel {
    position: absolute;
    top: 16px;
    right: 16px;
    width: 400px;
    max-height: calc(100% - 130px);
    background: rgba(250, 248, 245, 0.94);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    display: flex;
    flex-direction: column;
    border-radius: 6px;
    border: 1px solid rgba(180, 170, 155, 0.3);
    box-shadow: 0 2px 20px rgba(0, 0, 0, 0.15);
    overflow: hidden;
    z-index: 15;
  }

  #detail-panel.hidden {
    display: none;
  }

  #close-panel {
    position: absolute;
    top: 10px;
    right: 10px;
    width: 28px;
    height: 28px;
    background: rgba(40, 40, 40, 0.8);
    border: none;
    color: #fff;
    font-size: 1.1rem;
    cursor: pointer;
    z-index: 5;
    border-radius: 4px;
    transition: background 0.15s;
  }

  #close-panel:hover {
    background: rgba(60, 60, 60, 0.9);
  }

  #detail-image {
    width: 100%;
    height: 160px;
    object-fit: cover;
    background: rgba(200, 190, 175, 0.5);
    flex-shrink: 0;
  }

  #detail-image[src=""] {
    display: none;
  }

  #detail-content {
    flex: 1;
    padding: 16px 20px;
    overflow-y: auto;
    min-height: 0;
  }

  #detail-year {
    font-size: 0.75rem;
    color: #8b7355;
    margin-bottom: 6px;
    font-weight: 500;
  }

  #detail-title {
    margin: 0 0 12px 0;
    font-size: 1.25rem;
    color: #2a2520;
    line-height: 1.25;
    font-weight: 600;
  }

  #detail-author {
    display: block;
    font-size: 0.75rem;
    color: #666;
    margin-bottom: 12px;
  }

  #detail-author::before {
    content: "✏️ ";
  }

  #detail-author.hidden {
    display: none;
  }

  #detail-text {
    font-size: 0.9rem;
    line-height: 1.55;
    color: #444;
  }

  #detail-text :global(p) {
    margin-bottom: 0.85em;
  }

  #detail-text :global(blockquote) {
    margin: 0.85em 0;
    padding-left: 14px;
    border-left: 2px solid #c4a574;
    color: #666;
    font-style: italic;
  }

  #detail-text :global(a) {
    color: #8b5a2b;
  }

  #detail-text :global(img) {
    max-width: 100%;
    height: auto;
    border-radius: 4px;
    margin: 0.85em 0;
  }

  #detail-nav {
    display: flex;
    border-top: 1px solid rgba(180, 170, 155, 0.4);
    background: rgba(255, 255, 255, 0.5);
    flex-shrink: 0;
  }

  #detail-nav button {
    flex: 1;
    padding: 12px;
    background: transparent;
    border: none;
    font-size: 0.8rem;
    color: #666;
    cursor: pointer;
    transition: all 0.15s;
  }

  #detail-nav button:hover {
    background: rgba(255, 255, 255, 0.7);
    color: #333;
  }

  #detail-nav button:first-child {
    border-right: 1px solid rgba(180, 170, 155, 0.4);
  }

  /* Gallery in detail panel */
  #detail-gallery {
    margin-top: 16px;
    padding-top: 12px;
    border-top: 1px solid rgba(180, 170, 155, 0.4);
  }

  #detail-gallery:empty {
    display: none;
  }

  #detail-gallery h4 {
    font-size: 0.75rem;
    color: #8b7355;
    margin-bottom: 10px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  #detail-gallery :global(.detail-gallery-grid) {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 8px;
  }

  #detail-gallery :global(.detail-gallery-item) {
    display: flex;
    flex-direction: column;
    text-decoration: none;
    color: inherit;
    border-radius: 4px;
    overflow: hidden;
    background: rgba(200, 190, 175, 0.3);
    cursor: pointer;
    transition: transform 0.15s, box-shadow 0.15s;
  }

  #detail-gallery :global(.detail-gallery-item:hover) {
    transform: translateY(-1px);
    box-shadow: 0 2px 8px rgba(0,0,0,0.12);
  }

  #detail-gallery :global(.detail-gallery-item img) {
    width: 100%;
    height: 60px;
    object-fit: cover;
  }

  #detail-gallery :global(.detail-gallery-item .file-icon) {
    width: 100%;
    height: 60px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(200, 190, 175, 0.5);
    font-size: 1.5rem;
  }

  #detail-gallery :global(.detail-gallery-item .file-name) {
    padding: 4px 6px;
    font-size: 0.65rem;
    line-height: 1.2;
    color: #555;
    text-align: center;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  /* Lightbox */
  .lightbox {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.92);
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.2s, visibility 0.2s;
  }

  .lightbox.active {
    opacity: 1;
    visibility: visible;
  }

  .lightbox-content {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .lightbox-content img {
    max-width: 90vw;
    max-height: 85vh;
    object-fit: contain;
    border-radius: 4px;
  }

  .lightbox-content iframe {
    width: 85vw;
    height: 85vh;
    border: none;
    border-radius: 4px;
    background: white;
  }

  /* Hide inactive content type */
  .lightbox[data-type="image"] .lightbox-content iframe,
  .lightbox[data-type="pdf"] .lightbox-content img {
    display: none;
  }

  .lightbox-content img,
  .lightbox-content iframe {
    display: block;
  }

  .lightbox-close {
    position: absolute;
    top: 16px;
    right: 16px;
    width: 36px;
    height: 36px;
    background: rgba(255, 255, 255, 0.15);
    border: none;
    color: white;
    font-size: 1.3rem;
    cursor: pointer;
    border-radius: 50%;
    transition: background 0.15s;
  }

  .lightbox-close:hover {
    background: rgba(255, 255, 255, 0.25);
  }

  .lightbox-nav {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 44px;
    height: 44px;
    background: rgba(255, 255, 255, 0.15);
    border: none;
    color: white;
    font-size: 1.3rem;
    cursor: pointer;
    border-radius: 50%;
    transition: background 0.15s;
  }

  .lightbox-nav:hover {
    background: rgba(255, 255, 255, 0.25);
  }

  .lightbox-prev { left: 16px; }
  .lightbox-next { right: 16px; }

  .lightbox-caption {
    position: absolute;
    bottom: 16px;
    left: 50%;
    transform: translateX(-50%);
    color: white;
    font-size: 0.85rem;
    text-align: center;
    padding: 8px 14px;
    background: rgba(0, 0, 0, 0.6);
    border-radius: 4px;
    max-width: 80%;
  }

</style>
